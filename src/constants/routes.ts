export enum RoutePath {
  HOME = '/',
  LOGIN = 'login',
  SIGNUP = 'signup',
  CATALOG = 'catalog',
  BASKET = 'basket',
  ERROR = 'error',
  STORES = 'stores',
  LIKES = 'likes',
  GENERAL = '*',
}
